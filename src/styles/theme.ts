import { DefaultTheme } from 'styled-components';

export interface Colors {
  main: string;
  mainStrong: string;
  lineGray: string;
}

export const theme: DefaultTheme = {
  color: {
    main: '#DDEBFD',
    mainStrong: '#599BFC',
    lineGray: '#A1A1A1',
  },
};
